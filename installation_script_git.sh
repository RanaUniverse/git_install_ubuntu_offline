echo "This is installing 'git' in this machine."

sudo dpkg -i dependencies/*.deb
# sudo apt install ./dependencies/*.deb

echo "Git has been installed, Now you can use git command in this machine by terminal, for more git power use sudo apt install git-all"

